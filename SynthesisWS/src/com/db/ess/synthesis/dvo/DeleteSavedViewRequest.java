// Title:        DeleteSavedViewRequest
// Author:       Generate code
// Company:      
// Description:  This class was generated by the XML-Broker Developer Console
// Schema name:  /home/anthony/workspace/SynthesisWS/schema/Synthesis.xsd
// Java SDK:     

package com.db.ess.synthesis.dvo;

import java.util.Iterator;
import java.util.NoSuchElementException;
import java.io.IOException;

import org.adaptinet.sdk.adaptinetex.FastCacheException;
import org.adaptinet.sdk.fastcache.CacheServer;
import org.adaptinet.sdk.fastcache.DataArray;
import org.adaptinet.sdk.fastcache.DataItem;
import org.adaptinet.sdk.fastcache.FastCacheDVOBase;

public class DeleteSavedViewRequest implements FastCacheDVOBase  {
	public long getsavedViewsCount() { 
		return _savedViewsCount;
	}
	public SavedView[] getsavedViewsArray() { 
		return _savedViews;
	}
	public void setsavedViewsArray(SavedView newValue[]) { 
		_savedViews=newValue;
	}
	@SuppressWarnings("rawtypes")
	public Iterator getsavedViewsIterator() { 
		return new Iterator() {
			int cursor=0;
			int last=-1;
			public boolean hasNext() {
				if(_savedViews==null) return false;
				if(last<0) {
					last = _savedViews.length;
					while(--last>-1&&_savedViews[last]==null);
					last++;
				}
				return cursor!=last;
			}
			public Object next() {
				try {
					return _savedViews[cursor++];
				} catch(IndexOutOfBoundsException e) {
					throw new NoSuchElementException();
				}
			}
			public void remove() {
				try {
					_savedViews[cursor++]=null;
				} catch(IndexOutOfBoundsException e) {
					throw new NoSuchElementException();
				}
			}
		};
	}
	public SavedView getsavedViews(int idx) { 
		return (SavedView)_savedViews[idx];
	}
	public void setsavedViews(SavedView newValue) { 
		if(_savedViews!=null) {
			int __OPEN_A=-1;
			for(int __I_A=0;__I_A<_savedViews.length;__I_A++) {
				if(_savedViews[__I_A]==null) {
					__OPEN_A=__I_A;
					break;
				}
			}
			if(__OPEN_A<0) {
				__OPEN_A=_savedViews.length;
				SavedView array[] = new SavedView[__OPEN_A+10];
				System.arraycopy(_savedViews,0,array,0,_savedViews.length);
				_savedViews = array;
			}
			_savedViews[__OPEN_A] = newValue;
		}
		else {
			_savedViews = new SavedView[10];
			_savedViews[0] = newValue;
		}
		_savedViewsCount++;
	}
	public int getdefaultLocation() {
		return _defaultLocation;
	}
	public void setdefaultLocation(int newValue) {
		_defaultLocation = newValue;
	}
	public void putToCache(String key, CacheServer server) throws FastCacheException {
		DataArray da = writeCache();
		item.putDataArray(da);
		server.putValue(key, item);
		item.DataItemClear();
	}
	public void getFromCache(String key, CacheServer server) throws FastCacheException {
		DataItem item = server.getValue(key);
		DataArray da = new DataArray();
		item.getDataArray(da);
		readCache(da);
		item.DataItemClear();
	}
	public DataArray writeCache() throws FastCacheException {
		int sa_idx = 0;
		DataArray da = new DataArray(DataItem.DATAITEMDATAITEM, 2);
		if(_savedViews!=null) {
			DataArray daChild = new DataArray(DataItem.DATAITEMARRAY, _savedViewsCount);
			for(int __I_A=0;__I_A<_savedViewsCount;__I_A++) {
				daChild.setDataArray(__I_A, _savedViews[__I_A].writeCache());
			}
			item.putDataArray(daChild);
		}
		else {
			item.putEmpty();
		}
		da.setDataItem(sa_idx++, item);
		item.putInt(_defaultLocation);
		da.setDataItem(sa_idx++, item);
		item.DataItemClear();
		return da;
	}
	public void readCache(DataArray in) throws FastCacheException {
		int sa_idx = 0;
		DataArray daItem = new DataArray();
		DataArray daElement = new DataArray();
		in.getDataItem(sa_idx++, item);
		item.getDataArray(daItem);
		_savedViewsCount = daItem.getElements();
		if(_savedViewsCount>0) {
			_savedViews = new SavedView [(int)_savedViewsCount];
			for(int __I_A=0;__I_A<_savedViewsCount;__I_A++) {
				_savedViews[__I_A] = new SavedView();
				daItem.getDataArray(__I_A, daElement);
				_savedViews[__I_A].readCache(daElement);
			}
		}
		in.getDataItem(sa_idx++, item);
		_defaultLocation = item.getInt();
	}
	public void writeObject(byte [] data) throws FastCacheException {
		item.writeObject(data);
		DataArray in = new DataArray();
		item.getDataArray(in);
		readCache(in);
	}
	public byte [] readObject() throws FastCacheException, IOException {
		DataArray requestDA = writeCache();
		item.putDataArray(requestDA);
		return item.readObject();
	}
	private SavedView _savedViews[] = null;
	private long _savedViewsCount = 0;
	private int _defaultLocation;
	private DataItem item = new DataItem();
}

